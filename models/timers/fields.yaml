# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
        hidden: true
    timersgroups:
        label: Timer Group
        type: relation
        disabled: false
    name:
        label: Name
        disabled: false
    response:
        label: Response Supported variables {$user}, {$userurl}, {$usertitle}, {$usergame}, {$userviewers}, {$uptime}
        disabled: false
    command:
        label: Is a Command? If so ONLY put the custom command in response without (!)
        type: switch
        disabled: false        
