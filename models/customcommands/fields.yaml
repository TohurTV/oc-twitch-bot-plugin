# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
        hidden: true
    name:
        label: Name
        description: Name of the Command
        disabled: false
    command:
        label: Command without (!)
        description: command trigger without !
        disabled: false
    response:
        label: Response.  Supported variables {$user}, {$userurl}, {$usertitle}, {$usergame}, {$userviewers}, {$uptime}, {$targetuser}, {$targetuserurl}, {$targetusertitle}, {$targetusergame}, {$targetuserviewers}
        disabled: false
    description:
        label: Description
        disabled: false
    args:
        label: Requires Arguments?
        type: switch
        disabled: false